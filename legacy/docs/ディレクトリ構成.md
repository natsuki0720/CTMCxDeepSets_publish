# 公開版ディレクトリ構成

公開版の初期骨格として、以下の構成を作成した。

```text
.
├── src/
│   └── ctmc_surrogate/
│       ├── data/
│       │   ├── dataset.py
│       │   ├── dataset_csv_loader.py
│       │   └── collate.py
│       ├── features/
│       ├── models/
│       │   └── deepsets_regressor.py
│       ├── train/
│       ├── eval/
│       └── inference/
├── scripts/
│   └── data_generation/
│       └── entrypoint_gen_with_MLE.py
├── configs/
│   ├── dataset/
│   ├── model/
│   ├── train/
│   └── experiment/
├── tests/
├── docs/
│   └── assets/
└── artifacts/
```

## 実装済みモジュール（初期段階）
- `src/ctmc_surrogate/data/dataset.py`
  - 可変長集合入力と正値教師信号を扱う `CTMCSurrogateDataset` を定義。
- `src/ctmc_surrogate/data/dataset_csv_loader.py`
  - CTMC 合成データ CSV のパースと、学習・検証で再利用しやすい整形関数を定義。
- `src/ctmc_surrogate/data/collate.py`
  - パディング + マスク生成を行う `ctmc_collate_fn` を定義。
- `src/ctmc_surrogate/models/deepsets_regressor.py`
  - `DeepSetsAttnRegressor` と `build_model(model_config)` を定義。

空ディレクトリは `.gitkeep` を配置してGit管理対象としている。


## エントリーポイント配置ポリシー
- 実行専用の入口スクリプトは `scripts/` 配下に配置する。
